title: Test Rule - Single Weak Trigger
id: SIG-90010
status: experimental
description: A rule intentionally designed to trigger only once for weak scoring tests.
logsource:
  product: windows
detection:
  selection:
    EventID: 123456  # Rare event ID ensures minimal matches
    Source: "OnlyOnceEventSource"  # Uncommon key/value to limit triggering
  condition: selection
falsepositives:
  - none known
level: low
